import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the BlockTest component
 */
export const BlockTestStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(BlockTestStyles, {
    container: {
        fontSize: 22
    }
})

