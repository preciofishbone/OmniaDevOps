import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the BlockTestSettings component
 */
export const BlockTestSettingsStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(BlockTestSettingsStyles, {
    container: {
        fontSize: 22
    }
})

