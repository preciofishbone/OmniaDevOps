{
  "name": "Omnia.Breaking.Worker",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -v && tsc && omniafx build",
    "build-release": "npm run build -- --docs --minify"
  },
  "dependencies": {},
  "devDependencies": {
    "@omnia/tooling-composers": "6.0.0"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
